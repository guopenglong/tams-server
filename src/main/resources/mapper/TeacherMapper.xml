<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gpl.tams.dao.TeacherMapper">

<select id="getTeacherNameByTeacher" resultType="com.gpl.tams.model.data.TeacherDO">
    select name from t_teacher where name = '${name}' and password = '${password}'
    </select>
</mapper>
